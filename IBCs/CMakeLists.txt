ADD_LIBRARY(IBCs SHARED
    BC.cc
    free.cc
    outflow.cc
)

TARGET_INCLUDE_DIRECTORIES(IBCs PUBLIC "..")

DEAL_II_SETUP_TARGET(IBCs)

TARGET_LINK_LIBRARIES(IBCs utilities)

SET(CMAKE_EXPORT_COMPILE_COMMANDS ON)


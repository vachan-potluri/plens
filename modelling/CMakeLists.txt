ADD_LIBRARY(modelling SHARED
        NavierStokes.cc
)

TARGET_INCLUDE_DIRECTORIES(modelling PUBLIC "..")

DEAL_II_SETUP_TARGET(modelling)

TARGET_LINK_LIBRARIES(modelling utilities)

SET(CMAKE_EXPORT_COMPILE_COMMANDS ON)

